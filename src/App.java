import java.util.Arrays;

public class App {

  public static void main(String[] args) {
    PrintMethods noArgs = new PrintMethods();

    if (args.length == 0) {noArgs.NoArgsGivenStandardInfo();

    if (args.length < 0 && args[0] == "-"){
      if (args.toString().contains("l")){

      }
    }
    }
  }
}
